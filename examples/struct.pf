struct para {x, y}
let x = para with (1, 2);
let y = x.x;
struct okrag {srodek, promien}
let z = okrag with(
          para with (3, 4),
          5);

let pole = z.promien * z.promien * 3;

procedure foo(kolo) : kolo.promien * kolo.promien * 3 + kolo.srodek.x + kolo.srodek.y {}

let polesumasrodek = foo(z);